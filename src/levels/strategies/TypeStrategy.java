package levels.strategies;

import commands.MoveCommand;

public class TypeStrategy extends AbstractStrategy {
    @Override
    public boolean validMove(MoveCommand moveCommand) {
        //TODO type strategy move validation
        return false;
    }
}
