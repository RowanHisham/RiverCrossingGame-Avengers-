package levels.strategies;

import commands.MoveCommand;

public class WeightStrategy extends AbstractStrategy {
    @Override
    public boolean validMove(MoveCommand moveCommand) {
        //TODO weight strategy move validation
        return false;
    }
}
